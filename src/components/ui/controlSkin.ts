/** Compact form-control skin shared across the dense config editor: surface
 * fill, token rounding, token border. A different fill from the INPUT_SKIN used
 * by TextInput / Select, so it lives on its own. */
export const CONTROL_SKIN = "rounded-row border border-border bg-bg2";
